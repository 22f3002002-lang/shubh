name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Cache dependencies
        id: cache
        uses: actions/cache@v4
        with:
          path: ~/.npm
          key: cache-130dbb1

      - name: prime-cache-130dbb1
        run: echo "Cache result: ${{ steps.cache.outputs.cache-hit }}"

      # Add your actual build/test steps below if you have them
      # - name: Install dependencies
      #   run: npm install

      # - name: Run tests
      #   run: npm test
